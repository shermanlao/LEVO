# Frontend talks to Express on this machine. Production should set the public site origin
# and, if Next and Express are split, the API URL.
# NEXT_PUBLIC_STRAPI_API_URL=http://127.0.0.1:3333
# API_URL=http://127.0.0.1:3333

# Copy backend-server/.env.example into backend-server/.env for database settings.
