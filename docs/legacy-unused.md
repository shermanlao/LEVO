# Unused trees (`cms/` and `backend/`)

These folders are **not** part of `npm run dev` or `levo-deploy`. Do not start them.

| Path | What it was |
|------|-------------|
| `cms/` | Old Strapi 5 scaffold |
| `backend/` | Pre-`backend-server` JavaScript APIs (`index.js`, `minimal-api.js`, `admin-api.js` on port 3334) |

The live API is `backend-server/` on `127.0.0.1:3333`. Keep these folders only as archive; they can be deleted in a later cleanup.
